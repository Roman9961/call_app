{% extends 'base.content.html.twig' %}

{% block title %}CallingTask{% endblock %}

{% block content %}
    <h1>CallingTask</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ calling_task.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ calling_task.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('calling_task_index') }}">back to list</a>

    <a class="btn btn-success" href="{{ path('calling_task_edit', {'id': calling_task.id}) }}">edit</a>

    {{ include('calling_task/_delete_form.html.twig') }}
{% endblock %}
